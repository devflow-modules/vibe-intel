name: 📊 Relatório Semanal

on:
  schedule:
    - cron: "0 9 * * 1" # Toda segunda às 9h
  workflow_dispatch: # Execução manual permitida

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      # 1️⃣ Setup Node primeiro
      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # 2️⃣ Instalar PNPM usando o instalador oficial
      - name: 🧰 Instalar PNPM
        run: |
          corepack enable
          corepack prepare pnpm@10.0.0 --activate
          pnpm -v

      # 3️⃣ Instalar dependências
      - name: 📦 Instalar dependências
        run: pnpm install --no-frozen-lockfile

      # 4️⃣ Executar relatório
      - name: 🧠 Gerar Relatório Semanal (Notion)
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_SPRINT_DB_ID: ${{ secrets.NOTION_SPRINT_DB_ID }}
        run: pnpm tsx scripts/weekly-report.ts

      # 5️⃣ Commit automático
      - name: 💾 Commit Report
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add reports/
          git diff --cached --quiet || git commit -m "docs: relatório semanal automático"
          git push
